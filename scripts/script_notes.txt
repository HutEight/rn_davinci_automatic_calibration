

1. dual_frozen_evaluation.sh
   Used for the evaluation of the dual arm calibration. It drives the green arm to a set of point and then the yellow arm to the same set of point. The Polaris reading is recorded. 

2. dual_frozen_green.sh
   Used for collecting the cube data for psm green to Polaris calibration. 

3. dual_frozen_green_tom_polaris.sh
   Used for collecting the cube data for psm green to Polaris calibration. It bags a different topic due to changes made by Tom. 

4. green_arm_prismatic_offsets.sh
   Used for kinematic offset discovery and calibration.

 









